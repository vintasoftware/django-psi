# Requirements for running tests in Travis

codecov                   # Code coverage reporting
tox                       # Virtualenv management for tests
tox-travis                # Makes tox aware of travis env vars
zipp==1.1.0               # Pinned version to fix travisCI build